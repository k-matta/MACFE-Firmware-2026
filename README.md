# MACFE-Firmware-2026

## Hello World!
This is a simple Hello World program.

## Usage Instructions:
Compilation: Run `g++ main.cpp -o hello_world` (compile as `hello_world.exe` if on Windows) from the directory that has the `main.cpp` file.
Running: Run `./hello_world` (Linux) or `./hello_world.exe` (Windows) from the directory with the compiled executable.
